#include "js_support.h"
#include "jsnexus.h"

void js_Log(js_State *J) {

//    js_pushglobal(J);
//    js_getproperty(J, -1, "nexus");
//    auto ptr = js_touserdata(J, -1, "nexus");
//    auto nexusPtr = static_cast<JSNexus*>(ptr);
//    js_pop(J, 1);
//    nexusPtr->js_log(J);

    //js_get

//    const char *msg = js_tostring(J, 1);
//    std::string jsMsg = "JScript: " + std::string(msg);
//    Logger::Debug(jsMsg);
//    js_pushundefined(J);
}
